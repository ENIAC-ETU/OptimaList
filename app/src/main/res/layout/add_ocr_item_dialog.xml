<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingLeft="@dimen/activity_margin"
    android:paddingTop="@dimen/activity_margin"
    android:paddingRight="@dimen/activity_margin">

    <TextView
        android:id="@+id/add_ocr_item_dialog_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/dimen_10"
        android:fontFamily="sans-serif-medium"
        android:lineSpacingExtra="8sp"
        android:text="@string/new_item"
        android:textColor="@color/colorAccent"
        android:textSize="@dimen/lbl_new_shopping_list_title"
        android:textStyle="normal" />

    <AutoCompleteTextView
        android:id="@+id/add_ocr_item"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:background="@android:color/transparent"
        android:gravity="top"
        android:hint="@string/hint_enter_item_name"
        android:inputType="textCapSentences|textMultiLine"
        android:lines="4"
        android:textColorHint="@color/hint_enter_shopping_list_name"
        android:textSize="@dimen/input_new_shopping_list" />




    <Space
        android:layout_width="match_parent"

        android:layout_height="25dp" />


    <EditText
        android:id="@+id/ocr_price_input"
        android:layout_width="359dp"
        android:layout_height="62dp"
        android:ems="10"
        android:inputType="number"
        android:hint="fiyat"
        />
    <Spinner
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:id="@+id/add_ocr_item_category"/>



</LinearLayout>